# Backend API base URL — used by the browser to reach the API.
# This value is baked into the client bundle at Next.js build time.
# When building via Docker Compose, this is passed as a build ARG.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
